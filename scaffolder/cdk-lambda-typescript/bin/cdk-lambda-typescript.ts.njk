#!/usr/bin/env node
import * as cdk from 'aws-cdk-lib'
import 'source-map-support/register'
import { {{ projectName }} } from '../lib/{{ projectName }}-stack'

const app = new cdk.App()
new {{ projectName }}Stack(app, '{{ projectName }}Stack', {})
